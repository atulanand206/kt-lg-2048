package com.creations.games.engine.utils.logger

//Note: No value assignment needed if order is maintained.
// Comparison will use ordinal numbers
enum class LogLevel {
    TRACE,
    DEBUG,
    INFO,
    WARNING,
    ERROR,
    FATAL
}